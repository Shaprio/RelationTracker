<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Register</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 2rem;
            height: 100vh;
            margin: 0;
            background-color: #f9f9f9;
        }
        .left-section {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            width: 50%;
            text-align: center;
        }
        .logo {
            width: 200px;
            height: 150px;
            background-color: #ccc;
            margin-bottom: 1rem;
            display: flex;
            justify-content: center;
            align-items: center;
            font-weight: bold;
        }
        .left-section h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }
        .login-button {
            background-color: #1E3A5F;
            color: white;
            text-decoration: none;
            padding: 0.8rem 1.5rem;
            border-radius: 4px;
            font-size: 1rem;
            margin-top: 1rem;
        }
        .login-button:hover {
            background-color: #3b5c87;
        }
        .right-section {
            width: 40%;
            background-color: white;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .right-section h2 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }
        .form-group {
            margin-bottom: 1rem;
        }
        label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: bold;
        }
        input {
            width: 100%;
            padding: 0.8rem;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        .register-button {
            width: 100%;
            background-color: #007BFF;
            color: white;
            border: none;
            padding: 0.8rem;
            border-radius: 4px;
            font-weight: bold;
            cursor: pointer;
        }
        .register-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
<div class="left-section">
    <div class="logo">TEMP LOGO</div>
    <h1>Register</h1>
    <a href="{{ path('login') }}" class="login-button">Log In</a>
</div>
<div class="right-section">
    <h2>Register</h2>
    <form action="{{ path('register_submit') }}" method="POST">
        <div class="form-group">
            <label for="name">Name</label>
            <input type="text" id="name" name="name" required>
        </div>
        <div class="form-group">
            <label for="email">Email Address</label>
            <input type="email" id="email" name="email" required>
        </div>
        <div class="form-group">
            <label for="repeat-email">Repeat Email Address</label>
            <input type="email" id="repeat-email" name="repeat_email" required>
        </div>
        <div class="form-group">
            <label for="password">Password</label>
            <input type="password" id="password" name="password" required>
        </div>
        <button type="submit" class="register-button">Sign Up</button>
    </form>
</div>
</body>
</html>
